load("@rules_rust//rust:defs.bzl", "rust_binary")
load(":java.bzl", "java_bin")

rust_binary(
    name = "wrapper",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "@crates//:clap",
        "@crates//:libsui",
    ],
)

java_bin(
    name = "java",
)
