load("@rules_rust//rust:defs.bzl", "rust_binary")
load(":java.bzl", "java_bin")

rust_binary(
    name = "wrap",
    srcs = glob(["wrap/**/*.rs"]),
    deps = [
        "@crates//:serde",
        "@crates//:serde_json",
    ],
)

java_bin(
    name = "java",
)
